name: scrcpy_core
description: Pure Dart adb client library for the scrcpy GUI.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints: ^4.0.0
  test: ^1.31.0