name: scrcpy_daemon
description: Background tray daemon for the scrcpy GUI.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  scrcpy_core:
    path: ../core

dev_dependencies:
  lints: ^4.0.0
